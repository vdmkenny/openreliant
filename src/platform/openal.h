/* The OpenAL Soft interface the platform layer uses, translated to Zig by the build: the core API,
 * its extensions and EFX, statically linked. */
#define AL_LIBTYPE_STATIC
#define AL_ALEXT_PROTOTYPES
#include <AL/al.h>
#include <AL/alc.h>
#include <AL/alext.h>
#include <AL/efx.h>
